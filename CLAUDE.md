# 概要

AI 組織による開発チームを作成し、協業しタスクを完遂するコマンド

# 技術スタック

- Rust
- Claude Code CLI
- tmux

# コーディング規約

- TDD を採用するため、生成するコードは単体テストのコードから始める
- エラー文言・ハンドリングは、./src/error に**必ず**まとめ、共通で利用する
- 環境変数は、.env から取得し、./src/config でインポートする
- config はエントリーポイントで初期化し、初期化に失敗した場合はクラッシュする
- プロジェクトで使うコマンドは、Makefile にまとめ
